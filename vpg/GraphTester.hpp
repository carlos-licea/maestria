#ifndef GRAPHTESTER_HPP
#define GRAPHTESTER_HPP

// class Graph;

#include <vector>
#include <ostream>

class GraphTester {
public:
//     template<typename T>
//     GraphTester(Graph<T>& g);

//     Graph<T>& m_graph;
//     const int& m_size;
//     std::vector<float>& m_matrix;
};

// std::ostream& operator<<(std::ostream& o, GraphTester& g);

#endif //GRAPHTESTER_HPP
